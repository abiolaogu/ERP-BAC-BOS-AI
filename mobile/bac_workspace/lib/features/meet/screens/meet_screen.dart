import 'package:flutter/material.dart';

class MeetScreen extends StatelessWidget {
  const MeetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BAC Meet'),
      ),
      body: const Center(
        child: Text('Meet Screen - Coming Soon'),
      ),
    );
  }
}
